# wnba-pulse-web (Local Dev)

Minimal Next.js app to render `/daily` from the API.

## Quickstart
```bash
# set API URL
echo "NEXT_PUBLIC_API_URL=http://localhost:8000" > .env.local

# install deps
npm install

# dev
npm run dev
# open http://localhost:3000
```
